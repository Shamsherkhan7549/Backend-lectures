<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instagram</title>
</head>
<body>
    <div class="">
        <h1 class="text-2xl" >Instagram</h1>
        <form action="http://localhost:8080/home/search" method="get">
            <input type="text" name="username" placeholder="enter username">
            <button>submit</button>
        </form>

        <a href="http://localhost:8080/home/new">create new user</a>
        
      <% allData.map(item => { %>
            <div>
                <h2>@<%= item.username %></h2>
                <img src="<%= item.image %>" style="height: 200px;">
                <p><%= item.caption %></p> <a href="http://localhost:8080/home/<%= item.id%>">see in details</a>
            </div>
        <%}) %>
    </div>
</body>
</html>